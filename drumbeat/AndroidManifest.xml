<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.drumbeat.app"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="10" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" > >
        <activity
            android:name="com.drumbeat.app.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!--  Settings Activity -->
        <activity android:name=".SettingsActivity" />
        
        <!--  More Activity -->
        <activity android:name=".MoreActivity" />
        
        <!--  Tempo Activity -->
        <activity android:name=".TempoActivity" />
        
        <!--  Photos Activity -->
        <activity android:name=".FavoritesActivity" />
        
        <!--  ListView Level 1 Activity -->
        <activity android:name=".ListviewLevel1" />
        
        <!--  ListView Level 2 Activity -->
        <activity android:name=".ListviewLevel2" />
        
        <!--  Activity Stack Activity -->
        <activity android:name=".ActivityStack" />
        
    </application>

</manifest>